{
  "name": "metaphorex-site",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "cd .. && bun run scripts/build-neighbors.ts",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "astro": "^5"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
